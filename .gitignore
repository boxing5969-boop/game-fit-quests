# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build artifacts
node_modules
dist
dist-ssr
*.local

# Env / Secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# Cloudflare Wrangler / Pages Dev artifacts
.wrangler/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backup and broken files
*.bak
*.broken

# Claude Code local artifacts
.claude/

# Lockfile artifacts (project standard: bun.lock 텍스트 포맷만)
bun.lockb
package-lock.json

# AI / developer scratch reports (not for repo)
repomix-output.xml
coding-report.html
coding-report.pdf
coding-report.txt

# Nested clone artifacts (local dev quirk — 프로젝트 복제본이 생긴 경우)
game-fit-quests/

# Supabase CLI local cache (link/access token 흔적)
supabase/.temp/

# FUSE / OS temp files
.fuse_hidden*
_tmp_*

# 루트 레벨 talktalk 변종 (canonical: external/naver-talktalk/, supabase/functions/talktalk-*/)
# d9d1e74 에서 추적 해제 — 디스크에는 로컬 보존
/talktalk-webhook-*.js
/*-talktalk.js
/\[*\]talktalk.js

# 삭제할 파일을 모아두는 폴더 — 이 환경에서는 rm 이 막혀 있어 여기로 옮긴다.
# 실제 키가 담긴 백업 파일이 섞여 있어서, git add -A 로 통째로 올라가면 유출된다.
_to_delete/
