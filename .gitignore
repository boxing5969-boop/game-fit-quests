# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build artifacts
node_modules
dist
dist-ssr
*.local

# Env / Secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

# Cloudflare Wrangler / Pages Dev artifacts
.wrangler/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backup and broken files
*.bak
*.broken

# Claude Code local artifacts
.claude/

# Lockfile artifacts (project standard: bun.lock 텍스트 포맷만)
bun.lockb
package-lock.json

# AI / developer scratch reports (not for repo)
repomix-output.xml
coding-report.html
coding-report.pdf
coding-report.txt

# Nested clone artifacts (local dev quirk — 프로젝트 복제본이 생긴 경우)
game-fit-quests/

# Supabase CLI local cache (link/access token 흔적)
supabase/.temp/

# FUSE / OS temp files
.fuse_hidden*
_tmp_*

# 루트 레벨 talktalk 변종 (canonical: external/naver-talktalk/, supabase/functions/talktalk-*/)
# d9d1e74 에서 추적 해제 — 디스크에는 로컬 보존
/talktalk-webhook-*.js
/*-talktalk.js
/\[*\]talktalk.js
